export interface Positions {
  positionName: string;
  description?: string;
}

export interface PositionFull {
  positionId: number;
  positionName: string;
  description?: string;
}

export interface PositionsForm {
  positionName: string;
  description?: string;
}
